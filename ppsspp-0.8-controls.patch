diff -urN ppsspp-0.8-orig/native/base/PCMain.cpp ppsspp-0.8-patched/native/base/PCMain.cpp
--- ppsspp-0.8-orig/native/base/PCMain.cpp	2013-06-24 02:15:29.000000000 +1100
+++ ppsspp-0.8-patched/native/base/PCMain.cpp	2013-06-26 20:53:25.000000000 +1100
@@ -243,11 +243,9 @@
 #endif
 }
 
-
-
 InputState input_state;
 
-const int buttonMappings[14] = {
+int buttonMappings[14] = {
 #ifdef PANDORA
 	SDLK_PAGEDOWN,  //X => cross
 	SDLK_END,       //B => circle
@@ -258,19 +256,19 @@
 	SDLK_LALT,      //START
 	SDLK_LCTRL,     //SELECT
 #else
-	SDLK_z,         //A
-	SDLK_x,         //B
-	SDLK_a,         //X
-	SDLK_s,	        //Y
-	SDLK_q,         //LBUMPER
-	SDLK_w,         //RBUMPER
-	SDLK_SPACE,     //START
-	SDLK_v,	        //SELECT
+	SDLK_g,         //A
+	SDLK_h,         //B
+	SDLK_j,         //X
+	SDLK_k,	        //Y
+	SDLK_t,         //LBUMPER
+	SDLK_y,         //RBUMPER
+	SDLK_z,     //START
+	SDLK_x,	        //SELECT
 #endif
-	SDLK_UP,        //UP
-	SDLK_DOWN,      //DOWN
-	SDLK_LEFT,      //LEFT
-	SDLK_RIGHT,     //RIGHT
+	SDLK_w,        //UP
+	SDLK_s,      //DOWN
+	SDLK_a,      //LEFT
+	SDLK_d,     //RIGHT
 #ifdef PANDORA
 	SDLK_SPACE,     //MENU
 #else
@@ -279,7 +277,36 @@
 	SDLK_BACKSPACE,	//BACK
 };
 
+int leftJoyMappings[4] = {
+	SDLK_UP,
+	SDLK_DOWN,
+	SDLK_LEFT,
+	SDLK_RIGHT,
+};
+
+void SwapKeysAndJoy() {
+    int tmp_key;
+    tmp_key = buttonMappings[8];
+    buttonMappings[8] = leftJoyMappings[0];
+    leftJoyMappings[0] = tmp_key;
+
+    tmp_key = buttonMappings[9];
+    buttonMappings[9] = leftJoyMappings[1];
+    leftJoyMappings[1] = tmp_key;
+
+    tmp_key = buttonMappings[10];
+    buttonMappings[10] = leftJoyMappings[2];
+    leftJoyMappings[2] = tmp_key;
+
+    tmp_key = buttonMappings[11];
+    buttonMappings[11] = leftJoyMappings[3];
+    leftJoyMappings[3] = tmp_key;
+}
+
 void SimulateGamepad(const uint8 *keys, InputState *input) {
+	if (keys[SDLK_q])
+		SwapKeysAndJoy();
+
 	input->pad_buttons = 0;
 	input->pad_lstick_x = 0;
 	input->pad_lstick_y = 0;
@@ -303,13 +330,13 @@
 		}
 	}
 #else
-	if (keys[SDLK_i])
+	if (keys[leftJoyMappings[0]])
 		input->pad_lstick_y=1;
-	else if (keys[SDLK_k])
+	else if (keys[leftJoyMappings[1]])
 		input->pad_lstick_y=-1;
-	if (keys[SDLK_j])
+	if (keys[leftJoyMappings[2]])
 		input->pad_lstick_x=-1;
-	else if (keys[SDLK_l])
+	else if (keys[leftJoyMappings[3]])
 		input->pad_lstick_x=1;
 	if (keys[SDLK_KP8])
 		input->pad_rstick_y=1;
